# Workspace (raw API responses, cached data)
# Keep folder structure but ignore all contents
_workspace/*
!_workspace/.gitkeep

# Output (generated skill folders - users generate their own)
# Keep folder structure but ignore all contents
output/*
!output/.gitkeep

# Skill location — same rules for output/workspace generated at runtime
skills/website-to-skill-folder/output/*
!skills/website-to-skill-folder/output/.gitkeep
skills/website-to-skill-folder/_workspace/*
!skills/website-to-skill-folder/_workspace/.gitkeep

# Never commit a local API key inside the skill's scripts folder
skills/website-to-skill-folder/scripts/.env.local

# Development notes (internal documentation, not for public repo)
_dev-notes/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment variables (API keys, secrets)
.env.local
.env

# IDE
.claude/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
nul

# Backup/temporary files
*.backup
*.tmp
*.swp
*~
